<li class="user-section">
  <h3>Name: <%= user.name %></h3>

    <div class="profile-link">
      <button type="button" name="button" class="profile-button"><%= link_to 'See Profile',  user_path(user), class: 'profile-link' %></button>
      <%= render_add_friend(current_user, user, "add_friend") %>
    </div>

</li>
